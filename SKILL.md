---
name: local-ai-knowledge-base
description: 搭建"人定义、AI 维护"的本地 AI 协同知识库。从零创建一个基于 Obsidian 的 Agent-first 知识库：目录结构（原料/知识/产出）、三件套文件（规则说明书/导航地图/维护指南）、每日维护提示词、Windows 定时任务脚本、以及可选的"知识库管家"Agent 预设。用户请求"搭建知识库""创建 AI 知识库""整理个人知识库"或类似意图时使用。
---

# 本地 AI 协同知识库搭建法

> 把"人整理的一切"变成"人定义、AI 维护"。
> 方法论来源：Karpathy 的 LLM Knowledge Bases 模式（raw → LLM 编译 → Wiki → 输出 → 写回 → 健康检查）。

---

## 一、核心理念

| 传统知识库 (Human-first) | AI 知识库 (Agent-first) |
|--------------------------|--------------------------|
| 人整理、人查找、人消费 | 人定义目标与边界，AI 自发整理并直接使用 |
| 维护累、查找累、消费累 | 结构优先方便 AI 检索与执行 |

**一句话**：用户负责扔东西进来，AI 负责收拾整齐。

**四步模型**：
```
Raw(收集原料) → Schema(定规则) → Compile(编译知识) → Index(建索引)
```

---

## 二、目录结构（标准模板）

```
{知识库根目录}/                ← 例如 D:\我的知识库 或 E:\Docs\MyKB
├── 欢迎页.md              ← 新手向导（打开先看）
├── 导航地图.md            ← 内容目录（INDEX）
├── 规则说明书.md          ← 家规（SCHEMA）
├── 维护指南.md            ← 保养手册（MAINTENANCE）
├── 原料/                 ← 📥 原始资料（只读，不得改写删除）
│   ├── 网页摘录/          ← 收藏的文章、教程
│   ├── 灵感碎片/          ← 灵光一现的想法
│   └── 工作学习笔记/      ← 工作学习记录
├── 知识/                 ← 🧠 整理好的知识卡片
│   ├── 人物/             ← 关于"谁"
│   ├── 概念/             ← 关于"方法/知识"
│   └── 项目/             ← 关于"项目"
└── 产出/                 ← 📤 AI 干活的结果
    ├── 辅助创作/          ← AI 草稿、创作
    └── 维护日报/          ← 每日/每周维护报告
```

### 英文术语对照（写给小白看）

| 英文术语 | 中文名 | 大白话 |
|---------|--------|--------|
| `raw/` | 原料/ | 还没整理的原始资料 |
| `wiki/` | 知识/ | AI 整理好的知识卡片 |
| `outputs/` | 产出/ | AI 干活的成果 |
| `SCHEMA.md` | 规则说明书 | 知识库的家规 |
| `INDEX.md` | 导航地图 | 所有内容的目录 |
| `MAINTENANCE.md` | 维护指南 | 保养手册 |
| `[[Wiki Links]]` | 双向链接 | 知识之间的"绳子" |

---

## 三、三个核心文件模板

### 1. 规则说明书（SCHEMA.md）要点
- 目录结构说明（用大白话）
- 文件命名规则：
  - 人物/作品 → 直接叫名字（`{名字}.md`）
  - 概念/方法 → 叫名字（`怎么学{主题}.md`）
  - 项目 → 叫项目名（`{项目名}.md`）
  - 原料 → `YYYY-MM-DD_标题.md`
- 每张知识卡开头带元数据：`来源` / `时间` / `标签` / `不确定项`
- 安全边界（必写）：
  1. 原料原文不得改写或删除，只允许追加
  2. 只允许在知识库目录内工作
  3. 外部资料区（若有）只读
  4. 删除/覆盖/敏感/冲突 → 只列入待确认清单，不自动执行
  5. 重要判断由人拍板

### 2. 导航地图（INDEX.md）要点
- 分区列出：核心文件 / 知识卡片（人物、概念、项目）/ 原料 / 产出 / 待确认事项
- 维护提示：每次新增或修改知识页后同步更新

### 3. 维护指南（MAINTENANCE.md）要点
- 维护节奏表：

| 频率 | 执行者 | 内容 |
|------|--------|------|
| 每日 | AI | 扫原料 → 编译进知识 → 更新索引 → 生成日报 |
| 每周 | 人+AI | 检查变化/质量/待确认 |
| 每月 | 人 | 调来源、调标准、规划方向 |

- 每日 7 步流程：
  01 扫描新增源 → 02 记录来源 → 03 编译进知识卡片（用 `[[双向链接]]`）→ 04 更新索引 → 05 检查重复/冲突/过期 → 06 生成日报（列出改动+待确认）→ 07 敏感操作只入待确认清单

---

## 四、每日维护提示词（给任意 AI 用）

```
请执行{知识库名}的每日维护任务。
仓库路径：{绝对路径}

按 7 步执行：
01 扫描 原料/ 下上次维护后新增的文件
02 记录每个文件的来源：标题、链接、日期、原始路径
03 保留原文不变，把新信息编译进 知识/ 对应的文件夹（概念→知识/概念/，人物→知识/人物/，项目→知识/项目/），用 [[双向链接]] 连接相关页面
04 更新导航地图（INDEX），新增入口、刷新摘要
05 检查重复、冲突、过期信息和缺少来源的内容
06 在 产出/维护日报/ 生成 YYYY-MM-DD_维护日报.md，列出改了什么/新增什么/哪些需人工确认
07 涉及删除、覆盖、敏感信息或事实冲突时，只列入待确认清单，不自动执行

规则：
- 原料/ 原文不得改写或删除，只允许追加
- 知识卡片保留来源、时间和不确定项
- 只允许在 {绝对路径} 内工作
- 完成后汇报新增文件和待确认问题
```

---

## 五、Windows 定时任务（每日自动扫描）

### 脚本（daily-maintenance.ps1，纯英文内容避免编码问题）
关键点：
- **脚本源码保持纯 ASCII**：中文路径用 `$PSScriptRoot` 自动定位，或中文文案放独立的 UTF-8 模板文件（`Get-Content -Encoding UTF8` 读取）
- 原因：Windows 自带 PowerShell 5.1 对"无 BOM 的 UTF-8"脚本按 GBK 解码，中文会乱码破坏语法
- 生成日报到 `daily_reports/`（英文目录名更稳）

```powershell
# daily-maintenance.ps1 (ASCII-safe)
$base = $PSScriptRoot
$today = (Get-Date).ToString("yyyy-MM-dd")
$reportDir = Join-Path $base "daily_reports"
$reportPath = Join-Path $reportDir ($today + ".md")
# 扫描原料子目录的新增文件，读 UTF-8 模板生成中文日报
```

### 注册定时任务（set-daily-task.bat + setup-task.ps1）
- bat 用**纯英文内容** + `%~dp0` 相对路径引用 ps1（避免中文乱码）
- ps1 用 `New-ScheduledTaskAction` / `New-ScheduledTaskTrigger -Daily` / `Register-ScheduledTask` 注册每天 22:00 的任务
- 引用维护脚本时用**英文文件名**（如 `daily-maintenance.ps1`），避免中文文件名编码问题

---

## 六、（可选）"知识库管家"Agent 预设

在支持 Agent 预设的平台（如 DeepSeek Harness / DSH）上：
1. `copy 一个标准预设` 为 `{prefix}-assistant`
2. 改写预设的 `persona`：角色为"知识库管家"，写入知识库结构、7步维护流程、安全边界、沟通风格（中文大白话）
3. 更新 `preset.yml` 的 name/description
4. 用平台的预设校验机制验证（如 `standingKeyFor` / mount-validate）
5. 提醒用户：**新建的预设需要刷新 Web 界面或重启进程才会出现在选择器里**

---

## 七、交付清单（完成一个知识库要检查）

- [ ] 目录结构齐全（原料/知识/产出 + 三个核心 md）
- [ ] 规则说明书含安全边界
- [ ] 导航地图已建立且链接有效
- [ ] 维护指南含每日提示词
- [ ] （可选）定时任务脚本已测试运行
- [ ] 向用户说明日常用法：扔资料 → 说"帮我整理" → 看日报确认

---

## 八、写给使用者的日常上手话术

1. 平时：只往 `原料/` 扔资料（不用管分类）
2. 每天：说"帮我整理知识库"（或等定时任务生成日报后发给我）
3. 每周日：看报告，确认"待确认"事项
4. 每月1号：调方向、大扫除
5. 新领域：直接新建文件夹，或让 AI 建一个"板块"